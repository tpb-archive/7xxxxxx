its a direct play just open and play :D if it isnt than just install it with the CD-KEY,you may have to use cracked servers to,the version is 1.0 and you'll need to find a no-cd patch,but it should work without the patch